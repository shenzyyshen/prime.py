# Changelog

## [0.2.0]
- Added `primes_in_range(start, end)` to generate all primes between two numbers
- Added user input via `main()`

## [0.1.0]
- Initial `is_prime()` check with trial division
- Added `is_divisible_by()` helper
